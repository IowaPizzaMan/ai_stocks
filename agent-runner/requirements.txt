# LLM (direct Ollama structured-output calls — CrewAI dropped in Phase 3;
# see specs/component-specs/agent-runner/crew.md)
ollama>=0.4

# Data sources
fredapi>=0.5
finnhub-python>=2.4
requests>=2.32
lxml>=5.0
playwright>=1.48

# Analysis (indicators computed directly with pandas — pandas-ta 0.3.x was
# pulled from PyPI and 0.4.x is an incompatible rewrite; see tools/price.md)
pandas>=2.2
numpy>=1.26

# Storage / config
pymongo>=4.8,<5.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0

# tests
pytest>=8.0
mongomock>=4.1

# lint / format
ruff>=0.6
