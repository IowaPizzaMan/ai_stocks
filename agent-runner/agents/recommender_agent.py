"""Market flow timing — when to buy more or start selling.

Spec: specs/component-specs/agent-runner/agents/recommender_agent.md — implement per its build phase.
"""


def build_agent(tools: list | None = None):
    """Return the configured CrewAI Agent for RecommenderAgent."""
    raise NotImplementedError("recommender_agent: see spec")
