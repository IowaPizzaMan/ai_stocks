# Data Model: Remove Stock Page Horizontal Overflow

Not applicable. This feature is a frontend CSS/layout fix (flex-shrink behavior, a
responsive display breakpoint, and text-wrapping utilities) — it introduces no new
entities, fields, persisted state, or state transitions, and reads no data it didn't
already read. `spec.md`'s Key Entities section was correspondingly omitted for the same
reason.
